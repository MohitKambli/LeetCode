class Solution {
    public int scoreOfString(String s) {
        int n = s.length();
        int ans = 0;
        for(int i=0;i<n-1;i++) {
            int a = (int)s.charAt(i);
            int b = (int)s.charAt(i+1);
            ans += Math.abs(a-b);
        }
        return ans;
    }
}

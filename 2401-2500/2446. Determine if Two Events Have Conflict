class Solution {
    public boolean haveConflict(String[] event1, String[] event2) {
        int x = event2[0].compareTo(event1[1]);
        int y = event1[0].compareTo(event2[1]);
        return x <= 0 && y <= 0;
    }
}

class Solution {
    public boolean isPalindrome(int x) {
        int num = 0, temp = x;
        while(x > 0) {
            int d = x % 10;
            num = num * 10 + d;
            x /= 10;
        }
        return temp == num;
    }
}
